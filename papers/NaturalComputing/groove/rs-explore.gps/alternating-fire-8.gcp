// This fully explores the RS by alternation of the context and react rules
function fire() {
  context; react;
}

fire;
fire;
fire;
try { forbidden; }
else {
  fire;
  try { forbidden; }
  else {
    fire;
    try { forbidden; }
    else {
      fire;
      try { forbidden; }
      else {
        fire;
        try { forbidden; }
        else {
          fire;
          try { forbidden; }
          else { halt }
        }
      }
    }
  }
}
